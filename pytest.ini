[pytest]
testpaths = tests
addopts = -v --reruns=1 --reruns-delay=1
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    concurrency: marks tests as concurrency tests (run separately)